{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div id="root">Loading...</div>
        <div class="col-sm-6">
            <div class="form-group">
                <label for="name">Vardas:</label>
                <input class="form-control" id="name" type="text" name="name">
                <span id="nameValidation" data-path="{{ path('validatePerson', {'element': 'name'}) }}">:)</span>
            </div>
            <div class="form-group">
                <label for="team">Komanda:</label>
                <input class="form-control" id="team" type="text" name="team">
                <span id="teamValidation" data-path="{{ path('validatePerson', {'element': 'team'}) }}">:)</span>
            </div>
        </div>
    </div>
</div>
{% endblock %}
{% block javascripts %}
        <script>
            var test1 = true;
            var test2 = true;
        </script>
        <script src="{{ asset('build/js/validation.js') }}"></script>
        {{ parent() }}
{% endblock %}